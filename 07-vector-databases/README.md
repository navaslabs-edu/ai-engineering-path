# 07 - Vector Databases

## Objective

Store embeddings of a small set of documents in pgvector and query by similarity.

## Prerequisite

[06 - Embeddings](../06-embeddings/)

## Status

This session is planned but not yet written up. Notes, starter code, and exercises will follow the same structure as [01 - FastAPI Fundamentals](../01-fastapi-fundamentals/).

## Next

Once ready, this will lead into [08 - RAG - Naive](../08-rag-naive/).

**Note:** Session 07 is a checkpoint. Before Session 08 begins, the helpdesk API built across Sessions 02-07 is consolidated into a clean reference file (`checkpoint/main.py`) rather than being extended directly - RAG introduces enough new surface area (storage, retrieval) that it starts from a tidied base instead of a file with five sessions of accumulated changes.

